const ClienteEmpresa = require('./clientes.empresas.controller');
const PDV = require('./pdv.controller');
const POS = require('./pos.controller');
const Status = require('./status.controller');

module.exports = {
    ClienteEmpresa,
    PDV,
    POS,
    Status,
}